---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "lv/p5-pirma-masina-index");
if (!entry)
  throw new Error("Content entry 'lv/p5-pirma-masina-index' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/lv/esmu-vaditajs/pirma-masina/"
  alternateUrl="/ru/ya-voditel/pervaya-mashina/"
/>

---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "lv/p1-arzemniekiem-english");
if (!entry)
  throw new Error("Content entry 'lv/p1-arzemniekiem-english' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/lv/ka-iegut-tiesibas/arzemniekiem/english/"
  alternateUrl="/ru/kak-poluchit-prava/inostrancam/english/"
/>

---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "lv/p1-arzemniekiem-apmaina");
if (!entry)
  throw new Error("Content entry 'lv/p1-arzemniekiem-apmaina' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/lv/ka-iegut-tiesibas/arzemniekiem/apmaina/"
  alternateUrl="/ru/kak-poluchit-prava/inostrancam/obmen-prav/"
/>

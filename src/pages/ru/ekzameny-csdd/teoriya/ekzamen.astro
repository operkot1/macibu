---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "ru/p2-teorija-eksamens");
if (!entry) throw new Error("Content entry 'ru/p2-teorija-eksamens' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/ru/ekzameny-csdd/teoriya/ekzamen/"
  alternateUrl="/lv/csdd-eksameni/teorija/eksamens/"
/>

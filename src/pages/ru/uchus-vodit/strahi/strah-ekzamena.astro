---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "ru/p4-bailes-eksamena");
if (!entry) throw new Error("Content entry 'ru/p4-bailes-eksamena' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/ru/uchus-vodit/strahi/strah-ekzamena/"
  alternateUrl="/lv/macos-braukt/bailes/eksamena-bailes/"
/>

---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "ru/p5-stavvietas");
if (!entry) throw new Error("Content entry 'ru/p5-stavvietas' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/ru/ya-voditel/stoyanki/"
  alternateUrl="/lv/esmu-vaditajs/stavvietas/"
/>

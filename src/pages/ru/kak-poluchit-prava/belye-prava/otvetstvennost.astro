---
import { getEntry, render } from "astro:content";
import ArticleTemplate from "../../../../components/layout/ArticleTemplate.astro";

const entry = await getEntry("pages", "ru/p1-baltas-atbildiba");
if (!entry) throw new Error("Content entry 'ru/p1-baltas-atbildiba' not found");
const { Content } = await render(entry);
---

<ArticleTemplate
  entry={entry}
  Content={Content}
  canonicalPath="/ru/kak-poluchit-prava/belye-prava/otvetstvennost/"
  alternateUrl="/lv/ka-iegut-tiesibas/baltas-tiesibas/atbildiba/"
/>

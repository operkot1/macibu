---
route_id: "p1-soli-pa-solim"
lang: "lv"
title: "9 soļi līdz tiesībām"
description: "Pilns ceļš līdz autovadītāja apliecībai Latvijā — 9 soļi noteiktā secībā, ar progresa čeklistu."
pageType: "howto"
---

import StepListInteractive from "../../../components/content/StepListInteractive.tsx";
import HowToSchema from "../../../components/content/HowToSchema.astro";
import { nineSteps } from "../../shared/nineSteps";

export const steps = nineSteps.lv;

Pilns ceļš no pirmā ārsta apmeklējuma līdz plastikāta vadītāja apliecībai —
9 soļi noteiktā secībā. Atzīmē paveiktos — progress tiek saglabāts šajā
pārlūkā.

<StepListInteractive
  client:visible
  checklistId="p1-soli-pa-solim"
  steps={steps.map((s, i) => ({
    index: i + 1,
    title: s.title,
    ...(s.duration ? { durationLabel: s.duration } : {}),
  }))}
/>

<HowToSchema
  name="9 soļi līdz tiesībām"
  description="Pilns ceļš līdz autovadītāja apliecībai Latvijā — 9 soļi noteiktā secībā."
  steps={steps.map((s) => ({ name: s.title, text: s.text }))}
/>

## Kas notiek katrā solī

{steps.map((s, i) => (

  <div>
    <h3>{i + 1}. {s.title}</h3>
    <p>{s.text}</p>
  </div>
))}
